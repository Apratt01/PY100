How many arguments does the str.join method expect? What happens if you call it 
with fewer or more arguments?

str.join takes one argument an iterable
if you try to run it with more, or less, you will get an error.

