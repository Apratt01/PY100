Lists are mutable, tuples are not.
List literas use [ ] tuple literals use ( )

Both are sequences, which are ordered collections that use numeric index to
access.
Both are heterogeneous, elements can be any data type.
